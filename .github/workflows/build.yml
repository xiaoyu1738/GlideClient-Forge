name: Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v4
    - name: Set up Java 8
      uses: actions/setup-java@v4
      with:
        distribution: "temurin"
        java-version: 8
        cache: "gradle"

    - name: Build vanilla and Forge artifacts
      run: chmod +x ./gradlew && ./gradlew clean build reobfForgeJar --console=plain

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: GlideClient
        path: |
          build/libs/GlideClient-Release.jar
          build/libs/GlideClient-Release-Forge.jar
        if-no-files-found: error
